page { background: #f7f8fa; }
.add-title { color: #07c160; text-align: center !important; font-weight: 600; }
.popup-content { padding: 32rpx 0 60rpx; }
.popup-title { font-size: 34rpx; font-weight: 600; color: #333; display: block; padding: 0 32rpx 24rpx; text-align: center; }
.popup-row { display: flex; align-items: center; padding: 12rpx 32rpx; }
.popup-label { font-size: 28rpx; color: #666; width: 100rpx; flex-shrink: 0; }
.popup-actions { display: flex; gap: 16rpx; padding: 40rpx 32rpx 0; }
.popup-actions van-button { flex: 1; }
