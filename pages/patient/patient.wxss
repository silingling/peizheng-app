page { background: #f7f8fa; }
.page { padding-bottom: 140rpx; }
.btn-wrap { display: flex; gap: 16rpx; padding: 40rpx 16rpx; position: fixed; bottom: 0; left: 0; right: 0; background: #fff; }
.btn-half { flex: 1; }
.desc-wrap { padding: 16rpx; }
.desc-input { width: 100%; height: 120rpx; font-size: 28rpx; }
.price-label { font-size: 28rpx; font-weight: 600; }
.price-value { font-size: 36rpx; font-weight: bold; color: #ee0a24; }
.add-title { color: #07c160; text-align: center !important; }
